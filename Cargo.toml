[package]
name = "scan_fmt"
version = "0.2.0"
authors = ["wlentz"]
description = "A simple scanf()-like input for Rust"
repository = "https://github.com/wlentz/scan_fmt"
license = "MIT"
readme = "README.md"

[dependencies]
regex = "0.2.6"

[lib]
name = "scan_fmt"
path = "src/lib.rs"
